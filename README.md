# CSSSECOND-SEMESTER
Changes to the SQL script have been made, The ERD displays multiple many to many relationships but the SQL displays two many to many relationships reason being is, creating the tables with many foreign keys was challenging and certain data was not being displayed so changes had to be made to ensure the SQL displays everything including the enrolment samples.

![Action Workflow](Workflow.pnd)
